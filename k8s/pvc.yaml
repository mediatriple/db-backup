apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-backup-storage
spec:
  storageClassName: ""
  volumeName: db-backup-storage-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
